{% block header %}
    <nav class="navbar navbar-expand-lg bg-color">
        <a class="navbar-brand" href="{{ path('app_index') }}">Wild-series</a>


        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('category_index') }}">Catégories des séries </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('program_new') }}">Ajouter une série</a>
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        {{ app.user.email }}
                        <a class="nav-link" href="{{ path('my_space') }}">Mon compte</a>
                </li>
                <li>
                        <a class="nav-link" href="{{ path('app_logout') }}"> Se déconnecter</a>
                </li>
                    {% else %}
                <li>
                        <a class="nav-link" href="{{ path('app_register') }}"> Se connecter</a>
                    {% endif %}
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn-grad my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </nav>
{% endblock %}
